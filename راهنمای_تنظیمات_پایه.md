# راهنمای سیستم مدیریت تنظیمات پایه

## خلاصه
یک رابط کاربری مدرن و یکپارچه برای مدیریت تمام مدل‌های پایه پروژه شما در اپ `BaseInfo` ایجاد شده است.

## مدل‌های قابل مدیریت
این سیستم تمام مدل‌های زیر از اپ `BaseInfo` شما را مدیریت می‌کند:

1. **دستگاه‌های معدنی** (`MiningMachine`)
2. **بلوک‌های معدنی** (`MiningBlock`)
3. **دمپ‌ها** (`Dump`)
4. **خودروهای امدادی** (`EmergencyVehicle`)
5. **انواع سنگ معدنی** (`MineralType`)
6. **گروه‌های کاری** (`MachineryWorkGroup`)
7. **انواع دستگاه** (`TypeMachine`)

## ویژگی‌های کلیدی

### ۱. صفحه تب‌دار یکپارچه
- یک صفحه با ۷ تب به جای ۷ صفحه جداگانه
- جلوگیری از پراکندگی رابط کاربری
- تغییر سریع بین بخش‌ها بدون رفرش صفحه

### ۲. مدیریت دستگاه‌های معدنی (هوشمند)
#### دراپ‌داون آبشاری:
- وقتی گروه کاری را انتخاب می‌کنید، لیست نوع دستگاه به صورت خودکار فیلتر می‌شود
- از AJAX استفاده می‌کند

#### فیلد دینامیک پیمانکار:
- وقتی مالکیت را "پیمانکار" انتخاب می‌کنید، فیلد پیمانکار ظاهر می‌شود
- وقتی مالکیت را "شرکت" انتخاب می‌کنید، مخفی می‌شود

#### کلید تغییر وضعیت:
- یک کلید زیبای iOS-style برای فعال/غیرفعال کردن
- بدون نیاز به باز کردن مودال

#### جستجوی لحظه‌ای:
- جستجو در کد کارگاهی و نوع دستگاه
- با تاخیر ۳۰۰ میلی‌ثانیه

### ۳. مدیریت بلوک‌ها (با نشان‌های رنگی)
برای تشخیص سریع، از نشان‌های رنگی استفاده می‌شود:

**نوع بلوک:**
- 🟢 سنگ پرعیار - سبز
- ⚪ باطله - خاکستری
- 🔵 ترکیبی - آبی

**وضعیت بلوک:**
- 🟢 آماده بارگیری - سبز
- 🟡 در حال بارگیری - زرد
- ⚪ اتمام بلوک - خاکستری
- 🔵 آماده حفاری - آبی نیلی

### ۴. مدیریت خودروهای امدادی (با تقویم جلالی)
#### تقویم شمسی:
- برای تمام فیلدهای تاریخ از تقویم جلالی استفاده می‌شود
- تاریخ آخرین سرویس
- تاریخ سرویس بعدی
- تاریخ پایان بیمه
- تاریخ پایان معاینه فنی

#### چک‌لیست تجهیزات (۱۰ مورد):
✅ بوق و چراغ گردان
✅ شیلنگ و اتصالات
✅ مانیتور
✅ خاموش‌کننده دستی
✅ تجهیزات آتش‌نشانی
✅ پودر و فوم
✅ آب
✅ لاستیک
✅ سیستم ترمز
✅ سیستم روشنایی

#### نشان‌های وضعیت:
- 🟢 فعال - سبز
- 🔴 غیرفعال - قرمز
- 🟡 در حال تعمیر - زرد

### ۵. مدیریت ساده برای دیگر موارد
برای انواع سنگ، گروه‌های کاری، انواع دستگاه، و دمپ‌ها:
- فرم‌های ساده با نام و توضیحات
- عملیات CRUD کامل
- رابط کاربری تمیز و کاربرپسند

## دسترسی به سیستم

### مسیر URL:
```
http://localhost:8000/core/settings/base/
```

### از منوی سایدبار:
1. وارد سیستم شوید (با حساب کاربری staff یا admin)
2. به بخش "تنظیمات" بروید
3. روی "تنظیمات پایه" کلیک کنید

## نحوه استفاده

### افزودن رکورد جدید:
1. به تب مورد نظر بروید
2. روی دکمه "افزودن ... جدید" کلیک کنید
3. فرم را پر کنید
4. روی "ذخیره" کلیک کنید

### ویرایش رکورد:
1. در جدول، روی دکمه "ویرایش" رکورد مورد نظر کلیک کنید
2. اطلاعات را تغییر دهید
3. روی "ذخیره" کلیک کنید

### حذف رکورد:
1. روی دکمه "حذف" کلیک کنید
2. پیام تایید ظاهر می‌شود
3. روی "حذف" کلیک کنید تا تایید شود یا "انصراف" برای لغو

### جستجو (تب دستگاه‌ها):
1. در کادر جستجو تایپ کنید
2. جدول به صورت خودکار فیلتر می‌شود

### تغییر وضعیت فعال/غیرفعال (تب دستگاه‌ها):
1. روی کلید تغییر وضعیت کلیک کنید
2. وضعیت به صورت فوری تغییر می‌کند

## ویژگی‌های فنی

### Backend:
- **Framework**: Django
- **Authentication**: `@staff_member_required` برای تمام viewها
- **API Style**: RESTful با GET, POST, PUT, DELETE
- **Security**: CSRF protection و input validation

### Frontend:
- **Styling**: Tailwind CSS با پالت رنگی حرفه‌ای (نیلی)
- **Interactivity**: Alpine.js برای reactive components
- **Date Picker**: Jalali Date Picker از CDN
- **Direction**: RTL کامل برای فارسی

### مزایای طراحی:
- ✅ بدون رفرش صفحه (SPA-like experience)
- ✅ پشتیبانی از حالت تاریک
- ✅ Responsive برای موبایل
- ✅ انیمیشن‌های نرم
- ✅ پیام‌های موفقیت/خطا
- ✅ مودال‌های تایید برای حذف

## APIهای موجود

### دستگاه‌های معدنی:
```
GET/POST:  /core/api/machines/
GET/PUT/DELETE: /core/api/machines/<id>/
```

### بلوک‌ها:
```
GET/POST: /core/api/blocks/
GET/PUT/DELETE: /core/api/blocks/<id>/
```

### دمپ‌ها:
```
GET/POST: /core/api/dumps/
GET/PUT/DELETE: /core/api/dumps/<id>/
```

### خودروهای امدادی:
```
GET/POST: /core/api/emergency-vehicles/
GET/PUT/DELETE: /core/api/emergency-vehicles/<id>/
```

### انواع سنگ معدنی:
```
GET/POST: /core/api/mineral-types/
GET/PUT/DELETE: /core/api/mineral-types/<id>/
```

### گروه‌های کاری:
```
GET/POST: /core/api/workgroups/
GET/PUT/DELETE: /core/api/workgroups/<id>/
```

### انواع دستگاه:
```
GET/POST: /core/api/machine-types/
GET/PUT/DELETE: /core/api/machine-types/<id>/
```

### دراپ‌داون آبشاری:
```
GET: /core/api/machine-types-by-workgroup/<workgroup_id>/
```

## فایل‌های تغییر یافته

1. ✅ `core/views.py` - ۵۵۰+ خط کد backend
2. ✅ `core/urls.py` - ۲۰+ URL pattern
3. ✅ `templates/core/base_settings.html` - ۶۴۰ خط template
4. ✅ `templates/partials/sidebar.html` - منوی جدید
5. ✅ `CORE_SETTINGS_IMPLEMENTATION.md` - مستندات انگلیسی
6. ✅ `CORE_SETTINGS_SETUP.md` - راهنمای تست
7. ✅ `راهنمای_تنظیمات_پایه.md` - این فایل

## تست سیستم

### گام ۱: اجرای سرور
```bash
cd /home/mohamadkazem/HSC
python manage.py runserver
```

### گام ۲: لاگین
با حساب کاربری staff یا admin وارد شوید

### گام ۳: آزمایش تب‌ها
هر تب را باز کنید و عملیات زیر را امتحان کنید:
- ✅ افزودن رکورد جدید
- ✅ ویرایش رکورد موجود
- ✅ حذف رکورد (با تایید)
- ✅ جستجو (در تب دستگاه‌ها)
- ✅ تغییر وضعیت (در تب دستگاه‌ها)

### گام ۴: آزمایش ویژگی‌های خاص

**دراپ‌داون آبشاری:**
1. به تب دستگاه‌ها بروید
2. روی "افزودن دستگاه جدید" کلیک کنید
3. یک گروه کاری انتخاب کنید
4. ببینید لیست نوع دستگاه خودکار فیلتر می‌شود

**تقویم جلالی:**
1. به تب خودروهای امدادی بروید
2. روی "افزودن خودرو جدید" کلیک کنید
3. روی هر فیلد تاریخ کلیک کنید
4. تقویم شمسی باید ظاهر شود

**نشان‌های رنگی:**
1. چند بلوک با نوع و وضعیت‌های مختلف ایجاد کنید
2. در جدول ببینید که رنگ‌ها درست نمایش داده می‌شوند

## رفع مشکلات رایج

### مشکل: صفحه پیدا نشد (404)
**راه‌حل**: مطمئن شوید که URLهای core در فایل اصلی urls.py اضافه شده باشند:
```python
# در HSCprojects/urls.py
path('core/', include('core.urls', namespace='core')),
```
✅ این قبلاً اضافه شده است (خط ۴۹)

### مشکل: تقویم جلالی ظاهر نمی‌شود
**راه‌حل**: کنسول مرورگر را چک کنید. CDN باید در دسترس باشد.

### مشکل: خطای 500 در API
**راه‌حل**: لاگ‌های Django را چک کنید. احتمالاً مشکل foreign key است.

### مشکل: دراپ‌داون آبشاری کار نمی‌کند
**راه‌حل**: 
1. کنسول مرورگر را چک کنید
2. مطمئن شوید JavaScript فعال است
3. endpoint را مستقیماً در مرورگر باز کنید

## داده‌های نمونه برای تست

### گروه‌های کاری:
- حفاری
- بارگیری
- حمل و نقل
- تعمیر و نگهداری

### انواع دستگاه:
- بلدوزر (گروه: حفاری)
- لودر (گروه: بارگیری)
- کامیون معدن (گروه: حمل و نقل)
- دریل (گروه: حفاری)

### انواع سنگ معدنی:
- طلا
- مس
- آهن
- سنگ آهک

### نوع بلوک:
- باطله
- سنگ پرعیار
- ترکیبی

## نکات امنیتی
- ✅ تمام viewها نیاز به احراز هویت staff دارند
- ✅ محافظت CSRF فعال است
- ✅ اعتبارسنجی ورودی از طریق مدل‌های Django
- ✅ هیچ query خام SQL استفاده نشده
- ⚠️ می‌توانید rate limiting برای APIها اضافه کنید
- ⚠️ می‌توانید audit log برای عملیات حساس اضافه کنید

## پشتیبانی
برای مشکلات یا سوالات:
1. لاگ‌های Django را چک کنید: پوشه `logs/`
2. کنسول مرورگر را برای خطاهای JavaScript چک کنید
3. مستندات `CORE_SETTINGS_IMPLEMENTATION.md` را مطالعه کنید
4. راهنمای `CORE_SETTINGS_SETUP.md` را مطالعه کنید

## توسعه‌های آینده (اختیاری)
1. افزودن صفحه‌بندی برای دیتاست‌های بزرگ
2. قابلیت خروجی Excel/PDF
3. فیلترهای پیشرفته
4. عملیات گروهی (حذف، فعال/غیرفعال)
5. لاگ تغییرات (Audit Trail)
6. ایمپورت از CSV/Excel
7. نمایش گرافیکی چک‌لیست تجهیزات با آیکون
8. زمان‌بندی سرویس خودروها
9. گزارش کارکرد دستگاه‌ها
10. تایم‌لاین پیشرفت بلوک‌ها

## نتیجه‌گیری
این سیستم یک رابط کاربری قدرتمند، زیبا و کاربرپسند برای مدیریت تمام داده‌های پایه پروژه شما فراهم می‌کند. همه الزامات اجباری پیاده‌سازی شده‌اند:

✅ تب‌بندی یکپارچه
✅ فرم‌های هوشمند با دراپ‌داون آبشاری
✅ فیلدهای دینامیک
✅ نشان‌های رنگی
✅ کلیدهای تغییر وضعیت
✅ تقویم جلالی
✅ عملیات CRUD کامل
✅ مودال‌های تایید

سیستم آماده استفاده و تست است! 🚀

# راهنمای سبک پیام‌های اعلان

این راهنما به تیم‌ها کمک می‌کند تا متن اعلان‌ها را به شکل یکپارچه و قابل فهم برای کاربران نهایی بنویسند.

## اصول کلی

- **زبان:** فارسی روان با لحن رسمی ولی صمیمی.
- **ساختار پیام:** ابتدا منبع/موضوع، سپس اقدام یا وضعیت، و در صورت نیاز دلیل یا گام بعدی.
- **عنوان اعلان:** کوتاه (حداکثر ۶۵ کاراکتر) و خلاصه‌ی وضعیت؛ مثال: `ثبت گزارش روزانه`، `لغو جلسه`.
- **بدنه پیام:** شامل اطلاعات کلیدی (شناسه، تاریخ، شخص/دستگاه مربوطه). اگر اقدام لازم است، در انتهای پیام ذکر شود.
- **کاراکترهای عددی:** از اعداد فارسی استفاده شود (تابع `django.utils.formats.number_format` یا `persianize` در صورت نیاز).
- **لینک‌ها:** در صورت وجود مسیر جزئیات، فیلد `url` را مقداردهی کنید.
- **نوع اعلان (`notification_type`):**
  - `success`: عملیات موفق و بدون اقدام اضافی.
  - `info`: اطلاع‌رسانی عمومی.
  - `warning`: نیاز به توجه یا اقدام.
  - `error`: حالت بحرانی یا توقف فعالیت.

## الگوهای متنی پیشنهادی

| سناریو | عنوان | نمونه پیام |
|--------|-------|-------------|
| ایجاد گزارش جدید | `گزارش … ثبت شد` | `گزارش روزانه HSE گروه A در شیفت روزکار اول ثبت شد.` |
| مورد نیاز به توجه | `هشدار …` | `بارگیری ناایمن در بلوک ۳ توسط دستگاه L-12 گزارش شد.` |
| اقدام مدیریتی | `تأیید …` یا `رد …` | `درخواست مرخصی شما برای تاریخ ۱۴۰۳/۰۸/۲۰ تأیید شد.` |
| لغو/حذف | `لغو …` یا `حذف …` | `جلسه «بررسی ایمنی» برای تاریخ ۱۴۰۳/۰۸/۲۰ لغو شد. دلیل: عدم حضور مدیر.` |

## تبدیل پیام‌های موجود

برای بررسی پیام‌ها می‌توانید با اجرای اسکریپت زیر (در حال توسعه) عناوین و متن‌ها را استخراج کنید:

```bash
python manage.py list_notification_messages
```

(در صورت عدم وجود اسکریپت فوق، می‌توان با جستجوی `notify_` در مخزن پیام‌ها را مرور کرد.)

## فرآیند بازبینی با تیم HSE

1. تمامی پیام‌های اعلان را در این فایل یا `NOTIFICATION_ANALYSIS.md` ثبت و دسته‌بندی کنید.
2. تیم HSE پیام‌ها را مرور کرده و در صورت نیاز، متن جایگزین پیشنهاد می‌کند.
3. پیام‌های جدید به صورت Pull Request اعمال شوند تا تاریخچه تغییرات قابل پیگیری باشد.

---
برای پیشنهاد یا اصلاح این راهنما می‌توانید در فایل Pull Request مربوط به اعلان‌ها کامنت بگذارید.

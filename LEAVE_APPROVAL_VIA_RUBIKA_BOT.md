# تایید و رد درخواست‌های مرخصی از طریق ربات روبیکا

## خلاصه
این فیچر امکان تایید و رد درخواست‌های مرخصی را از طریق ربات روبیکا فراهم می‌کند. هم جانشین‌ها و هم مدیران تایید کننده می‌توانند مستقیماً از طریق ربات به درخواست‌ها پاسخ دهند.

## فلوی کلی

### 1. ثبت درخواست مرخصی
- کاربر درخواست مرخصی را از طریق وب یا ربات ثبت می‌کند
- سیستم بر اساس نوع مرخصی، مسیر تایید را تعیین می‌کند:
  - **مرخصی استحقاقی**: نیاز به تایید جانشین → تایید مدیر
  - **غیبت، استعلاجی، ساعتی**: مستقیم به مدیر می‌رود

### 2. ارسال نوتیفیکیشن به جانشین (برای مرخصی استحقاقی)
- نوتیفیکیشن به داشبورد وب ارسال می‌شود
- پیام با دکمه‌های تایید/رد به ربات روبیکا ارسال می‌شود
- جانشین می‌تواند:
  - ✅ **تایید کند**: درخواست به مدیر ارسال می‌شود
  - ❌ **رد کند**: باید دلیل رد را وارد کند

### 3. ارسال نوتیفیکیشن به مدیر
- بعد از تایید جانشین یا برای انواع مرخصی که نیاز به جانشین ندارند
- نوتیفیکیشن به داشبورد وب ارسال می‌شود
- پیام با دکمه‌های تایید/رد به ربات روبیکا ارسال می‌شود
- مدیر می‌تواند:
  - ✅ **تایید کند**: درخواست تایید نهایی می‌شود
  - ❌ **رد کند**: باید دلیل رد را وارد کند

### 4. اطلاع‌رسانی به درخواست‌دهنده
- در هر مرحله از تایید یا رد، نوتیفیکیشن به درخواست‌دهنده ارسال می‌شود
- شامل نام تایید کننده/رد کننده و دلیل رد (در صورت وجود)

## فایل‌های تغییر یافته

### 1. `leave_reports/utils.py`
#### تغییرات:
- **تابع `send_notification_to_replacement`**: افزوده شد قابلیت ارسال پیام با دکمه‌های تایید/رد به ربات
- **تابع `send_notification_to_manager`**: افزوده شد قابلیت ارسال پیام با دکمه‌های تایید/رد به ربات
- **تابع جدید `send_leave_approval_notification_to_rubika`**: ارسال درخواست تایید به ربات از طریق Celery task

### 2. `rubika_bot/tasks.py`
#### تغییرات:
- **تابع جدید `send_leave_approval_request`**: Celery task برای ارسال پیام درخواست تایید با دکمه‌ها
  - ساختن متن پیام با جزئیات کامل درخواست
  - ساختن دکمه‌های تایید و رد با ID های منحصر به فرد
  - ارسال پیام به ربات روبیکا

### 3. `rubika_bot/services.py`
#### تغییرات:

##### در `_handle_button`:
- افزوده شد handler برای دکمه‌های با فرمت `approve_leave_*` و `reject_leave_*`
- افزوده شد handler برای دکمه `cancel_rejection`

##### توابع جدید:

1. **`_handle_leave_approval_action`**
   - پردازش کلیک روی دکمه‌های تایید/رد
   - بررسی دسترسی کاربر
   - Parse کردن button_id برای شناسایی نوع عملیات و درخواست
   - هدایت به توابع مربوطه

2. **`_ask_rejection_reason`**
   - درخواست دلیل رد از کاربر
   - ذخیره state برای پردازش پاسخ بعدی

3. **`_process_leave_approval`**
   - پردازش تایید درخواست
   - به‌روزرسانی وضعیت درخواست در دیتابیس
   - ارسال نوتیفیکیشن‌های لازم

4. **`_process_rejection_reason`**
   - پردازش دلیل رد وارد شده
   - به‌روزرسانی وضعیت درخواست به 'rejected'
   - ارسال نوتیفیکیشن به درخواست‌دهنده

5. **`_cancel_rejection`**
   - لغو فرایند رد درخواست
   - Reset کردن state

##### در `_handle_plain_text`:
- افزوده شد بررسی برای state `rejection_reason`
- هدایت به `_process_rejection_reason` در صورت وارد کردن دلیل رد

### 4. `rubika_bot/signals.py`
#### تغییرات:
- **بهبود signal `send_notification_to_rubika`**:
  - افزودن آیکون‌های مناسب برای هر نوع نوتیفیکیشن (✅ ⚠️ ❌ ℹ️ 📅)
  - فرمت بهتر پیام با عنوان، متن و لینک
  - فیلتر کردن نوتیفیکیشن‌های درخواست تایید مرخصی (که با دکمه ارسال می‌شوند)
  - ارسال خودکار تمام نوتیفیکیشن‌های دیگر به ربات روبیکا

**نکته مهم**: نوتیفیکیشن‌هایی که عنوان آن‌ها شامل "درخواست جایگزینی" یا "درخواست تایید" است، از این signal نادیده گرفته می‌شوند چون آن‌ها با `send_leave_approval_request` task و همراه با دکمه‌های تایید/رد ارسال می‌شوند.


## فرمت Button IDs

### برای تایید:
```
approve_leave_{approval_type}_{leave_id}
```
مثال: `approve_leave_replacement_123` یا `approve_leave_manager_456`

### برای رد:
```
reject_leave_{approval_type}_{leave_id}
```
مثال: `reject_leave_replacement_123` یا `reject_leave_manager_456`

پارامترها:
- `approval_type`: `replacement` یا `manager`
- `leave_id`: شناسه عددی درخواست مرخصی

## نمونه پیام‌های ارسالی

### پیام برای جانشین:
```
🔔 درخواست جایگزینی مرخصی

👤 درخواست‌دهنده: احمد احمدی
📌 نوع مرخصی: مرخصی استحقاقی
📅 تاریخ: 1403-09-15
🕐 شیفت: روزکار اول

📝 توضیحات: نیاز به استراحت

❓ لطفاً تصمیم خود را اعلام کنید:

[✅ تایید]  [❌ رد]
```

### پیام برای مدیر:
```
🔔 درخواست تایید مرخصی

👤 درخواست‌دهنده: احمد احمدی
📌 نوع مرخصی: مرخصی استحقاقی
📅 تاریخ: 1403-09-15
🕐 شیفت: روزکار اول
👥 جایگزین: علی علوی

📝 توضیحات: نیاز به استراحت

❓ لطفاً تصمیم خود را اعلام کنید:

[✅ تایید]  [❌ رد]
```

### پیام درخواست دلیل رد:
```
📝 دلیل رد درخواست

لطفاً دلیل رد این درخواست را وارد کنید:

(این دلیل به درخواست‌دهنده نمایش داده می‌شود)

[❌ انصراف]
```

## Flow States

### State های LeaveRequestState:
- `idle`: حالت پیش‌فرض
- `rejection_reason`: در انتظار دریافت دلیل رد
- سایر state ها برای فرایند ثبت درخواست مرخصی (بدون تغییر)

### داده‌های ذخیره شده در state برای رد:
```python
{
    'leave_id': '123',
    'approval_type': 'replacement'  # or 'manager'
}
```

## نوتیفیکیشن‌های ارسالی

### 1. به جانشین (زمان ثبت درخواست):
- نوتیفیکیشن داشبورد: `notification_type='warning'`
- پیام ربات: با دکمه‌های تایید/رد

### 2. به مدیر (بعد از تایید جانشین یا برای انواع دیگر):
- نوتیفیکیشن داشبورد: `notification_type='info'`
- پیام ربات: با دکمه‌های تایید/رد

### 3. به درخواست‌دهنده (بعد از تایید جانشین):
- نوتیفیکیشن داشبورد: `notification_type='info'`
- پیام ربات: اطلاع از تایید جانشین (با آیکون ℹ️)

### 4. به درخواست‌دهنده (بعد از تایید نهایی مدیر):
- نوتیفیکیشن داشبورد: `notification_type='success'`
- پیام ربات: اطلاع از تایید نهایی (با آیکون ✅)

### 5. به درخواست‌دهنده (بعد از رد):
- نوتیفیکیشن داشبورد: `notification_type='error'`
- پیام ربات: اطلاع از رد با دلیل (با آیکون ❌)

## ارسال خودکار تمام نوتیفیکیشن‌ها به ربات

### قابلیت جدید:
✅ **تمام نوتیفیکیشن‌های سیستم** به صورت خودکار به ربات روبیکا ارسال می‌شوند

### ویژگی‌ها:
- 🎨 **آیکون‌های مناسب** برای هر نوع نوتیفیکیشن:
  - ℹ️ اطلاع‌رسانی (info)
  - ✅ موفقیت (success)
  - ⚠️ هشدار (warning)
  - ❌ خطا (error)
  - 📅 جلسه (meeting)

- 📝 **فرمت بهتر پیام**:
  - عنوان با آیکون
  - متن پیام
  - لینک برای مشاهده جزئیات (در صورت وجود)

- 🎯 **فیلترینگ هوشمند**:
  - نوتیفیکیشن‌های درخواست تایید مرخصی (که با دکمه ارسال می‌شوند) از ارسال دوباره جلوگیری می‌شود
  - بقیه نوتیفیکیشن‌ها به صورت خودکار ارسال می‌شوند

### مثال پیام ارسالی:
```
✅ تأیید نهایی مرخصی

درخواست مرخصی استحقاقی شما برای تاریخ 1403-09-15 توسط علی علوی تایید شد.

🔗 برای مشاهده جزئیات به پنل وب مراجعه کنید.
```


## بررسی دسترسی

### برای جانشین:
```python
if leave_request.replacement_person == user and leave_request.status == 'pending_replacement':
    # مجاز به تایید/رد
```

### برای مدیر:
```python
if leave_request.can_be_approved_by(user) and leave_request.status == 'pending_approval':
    # مجاز به تایید/رد
```

## وضعیت‌های درخواست

1. **`pending_replacement`**: منتظر تایید جانشین
2. **`pending_approval`**: منتظر تایید مدیر
3. **`approved`**: تایید شده
4. **`rejected`**: رد شده

## نکات امنیتی

1. ✅ بررسی اتصال کاربر به سیستم قبل از هر عملیات
2. ✅ بررسی دسترسی کاربر به درخواست
3. ✅ بررسی وضعیت فعلی درخواست
4. ✅ استفاده از transaction برای consistency
5. ✅ Logging کامل تمام عملیات

## نحوه استفاده

### برای کاربران:
1. کاربر درخواست مرخصی را ثبت می‌کند
2. جانشین/مدیر پیام را در ربات روبیکا دریافت می‌کند
3. با کلیک روی دکمه تایید یا رد، عملیات انجام می‌شود
4. در صورت رد، دلیل رد را وارد می‌کند
5. نوتیفیکیشن به درخواست‌دهنده ارسال می‌شود

### پیش‌نیازها:
- کاربر باید به ربات روبیکا متصل باشد (connect)
- کاربر باید دسترسی لازم را داشته باشد (جانشین یا مدیر)
- Celery باید در حال اجرا باشد

## تست

برای تست این فیچر:

1. **تست تایید جانشین:**
   - یک درخواست مرخصی استحقاقی ثبت کنید
   - بررسی کنید که جانشین پیام با دکمه‌ها را دریافت کند
   - روی دکمه تایید کلیک کنید
   - بررسی کنید که وضعیت به `pending_approval` تغییر کند
   - بررسی کنید که مدیر نوتیفیکیشن دریافت کند

2. **تست رد جانشین:**
   - یک درخواست مرخصی استحقاقی ثبت کنید
   - روی دکمه رد کلیک کنید
   - دلیل رد را وارد کنید
   - بررسی کنید که وضعیت به `rejected` تغییر کند
   - بررسی کنید که درخواست‌دهنده نوتیفیکیشن با دلیل رد دریافت کند

3. **تست تایید مدیر:**
   - یک درخواست در وضعیت `pending_approval` ایجاد کنید
   - بررسی کنید که مدیر پیام با دکمه‌ها را دریافت کند
   - روی دکمه تایید کلیک کنید
   - بررسی کنید که وضعیت به `approved` تغییر کند

4. **تست رد مدیر:**
   - یک درخواست در وضعیت `pending_approval` ایجاد کنید
   - روی دکمه رد کلیک کنید
   - دلیل رد را وارد کنید
   - بررسی کنید که وضعیت به `rejected` تغییر کند

5. **تست دسترسی:**
   - تلاش کنید درخواستی را که مربوط به شما نیست تایید کنید
   - بررسی کنید که پیام خطای دسترسی دریافت شود

## لاگ‌ها

تمام عملیات در لاگ‌ها ثبت می‌شوند:

- ارسال درخواست تایید به ربات
- کلیک روی دکمه‌های تایید/رد
- خطاهای دسترسی
- خطاهای سیستمی

برای مشاهده لاگ‌ها:
```bash
# در ترمینال سرور Django
tail -f logs/django.log

# در ترمینال Celery
tail -f logs/celery.log
```

## خطاهای احتمالی و راه حل

### 1. پیام با دکمه‌ها ارسال نمی‌شود
- بررسی کنید که Celery در حال اجرا باشد
- بررسی کنید که توکن ربات صحیح باشد
- لاگ‌های Celery را بررسی کنید

### 2. دکمه‌ها کار نمی‌کنند
- بررسی کنید که webhook به درستی تنظیم شده باشد
- بررسی کنید که کاربر به سیستم متصل باشد
- لاگ‌های Django را بررسی کنید

### 3. پیام "دسترسی ندارید" نمایش داده می‌شود
- بررسی کنید که کاربر جانشین یا مدیر صحیح باشد
- بررسی کنید که وضعیت درخواست صحیح باشد
- جدول `ApprovalHierarchy` را بررسی کنید

## تغییرات آینده (پیشنهادی)

1. افزودن امکان مشاهده لیست درخواست‌های منتظر تایید در ربات
2. افزودن نوتیفیکیشن یادآوری برای درخواست‌های تایید نشده
3. افزودن امکان ویرایش درخواست قبل از تایید نهایی
4. افزودن آمار و گزارش تایید/رد درخواست‌ها

## نویسنده
تاریخ: 2025-11-10
نسخه: 1.0.0
